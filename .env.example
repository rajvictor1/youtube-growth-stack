# Server-only provider credentials. Never prefix these with NEXT_PUBLIC_.
OPENAI_API_KEY=
YOUTUBE_API_KEY=
APIFY_API_TOKEN=
APIFY_ACTOR_ID=h7sDV53CddomktSi5
FIRECRAWL_API_KEY=

# Supabase URL and anon key are safe for the browser; the service role is not.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Shared only with the trusted scheduler that invokes POST /api/jobs/process.
# Generate at least 32 random characters; never prefix this with NEXT_PUBLIC_.
JOB_WORKER_SECRET=
